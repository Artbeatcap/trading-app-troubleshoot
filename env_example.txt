# Example environment variables

# Flask Configuration
SECRET_KEY=your-secret-key-here
FLASK_ENV=development

# Database (PostgreSQL recommended for production)
DATABASE_URL=postgresql://localhost/tradingapp

# OpenAI API (for AI analysis)
OPENAI_API_KEY=your-openai-api-key

# Market data provider (required)
# Polygon.io ("Massive API"). Accepts MASSIVE_API_KEY or POLYGON_API_KEY.
# Earnings calendar, earnings surprises, and economic calendar are NOT
# available on the Stocks Starter tier and return empty results.
MASSIVE_API_KEY=your-polygon-api-key
# Optional base-URL override (mainly for testing)
POLYGON_BASE_URL=https://api.polygon.io
# Seconds between outbound API calls (Polygon Stocks Starter = 0.05 is safe, free tier = 12)
RATE_LIMIT_DELAY=0.05

# Anthropic Claude (for AI catalyst summaries on movers/alerts)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Google OAuth Configuration
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret

# Email Configuration (optional)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
# Mail sender configuration
MAIL_DEFAULT_SENDER_NAME=Options Plunge Support
MAIL_DEFAULT_SENDER_EMAIL=support@optionsplunge.com

# File Upload Settings
UPLOAD_FOLDER=static/uploads
MAX_CONTENT_LENGTH=16777216  # 16MB
ALLOWED_EXTENSIONS=png,jpg,jpeg,gif,pdf
